# 11.2 — 为什么信任属于类型体系

> **状态（Status）：** 提案论证（PROPOSED rationale）  
> **概述（Summary）：** 信任状态重要到必须出现在接口中，因为忘记一步验证是一类编程错误，而不只是日志记录上的关切。

审查者应当能够检视一个函数签名，区分“来自模型的值”与“通过了显式验证器的值”。

这种区分也为工具创造了空间：诊断可以解释缺失的边界，IDE 可以把信任流动可视化，
审计可以搜索那些来源不完整的已验证值。
