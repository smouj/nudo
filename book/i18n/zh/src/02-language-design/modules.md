# 02.3 — 模块与程序边界

> **状态（Status）：** 提案中（PROPOSED）  
> **概述（Summary）：** 模块必须提供显式的命名空间和可复现的程序结构，同时不隐藏授权或依赖边界。

模块设计与包设计、名称解析和公开接口紧密相关。NUDO 应当避免隐式的全局命名空间，
并让公开 API 面成为有意的选择。

开放的设计工作包括：包清单（manifest）、可见性、依赖解析，
以及依赖所声明的能力（capability）如何暴露给调用方。
